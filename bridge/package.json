{
  "name": "@absolutejs/machines-bridge",
  "version": "0.2.0-beta.0",
  "description": "Shop-LAN bridge agent for @absolutejs/commerce-machines: holds one live socket to your app, delivers pushed print/embroidery jobs to a hot folder, a printer port (TCP 9100), an IPP printer or the OS print queue, and pushes machine run telemetry back (production reports, Zebra alerts, SNMP traps, RIP webhooks). No polling, no arbitrary command execution.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/absolutejs/commerce-adapters.git",
    "directory": "bridge"
  },
  "homepage": "https://github.com/absolutejs/commerce-adapters/tree/main/bridge",
  "bugs": {
    "url": "https://github.com/absolutejs/commerce-adapters/issues"
  },
  "files": [
    "dist",
    "README.md",
    "install.md",
    "LICENSE"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "absolutejs-machines-bridge": "./dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "license": "Apache-2.0",
  "author": "Alex Kahn",
  "engines": {
    "bun": ">=1.1"
  },
  "scripts": {
    "build": "rm -rf dist && bun build ./src/index.ts ./src/cli.ts --root ./src --outdir dist --target bun --external @absolutejs/commerce-machines --external '@absolutejs/commerce-machines/*' --external @absolutejs/sync --external '@absolutejs/sync/*' && tsc --emitDeclarationOnly --project tsconfig.json && chmod +x dist/cli.js",
    "format": "prettier --write \"./**/*.{js,ts,json,md}\"",
    "release": "bun run check:package && npm publish --access public --tag beta",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check:package": "bun run format && bun run typecheck && bun run test && bun run build"
  },
  "dependencies": {
    "@absolutejs/commerce-machines": "0.3.0-beta.0",
    "@absolutejs/sync": "^2.30.0"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "prettier": "3.9.6",
    "typescript": "5.9.3"
  }
}
